# Generate with: openssl rand -hex 32
SESSION_SIGNING_SECRET=replace-with-64-or-more-random-hex-characters
# Comma-separated HMAC-SHA-256 digests of one-time beta invite codes. Never put codes here.
# Generate with the command in README after setting SESSION_SIGNING_SECRET.
BETA_INVITE_HASHES=
SARVAM_API_KEY=
OPENAI_API_KEY=
MAX_AUDIO_BYTES=10485760
MAX_AUDIO_SECONDS=120
DAILY_TRANSCRIPT_LIMIT=60
REQUESTS_PER_MINUTE=20
